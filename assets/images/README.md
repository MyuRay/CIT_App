# Bus Timetable Image

この フォルダに `bus_timetable.png` ファイルを配置してください。

提供された画像は「令和7年度前期バスダイヤ」の時刻表です。

## 設定手順
1. 提供された画像を `bus_timetable.png` という名前で保存
2. このフォルダ（`assets/images/`）に配置
3. `flutter pub get` でアセットを更新
4. アプリをリビルド

## 推奨画像サイズ
- PNG形式推奨
- 高解像度（できれば1200px以上）
- 文字が読みやすい品質